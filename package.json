{
  "name": "love-story-repack",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "all": "yarn --cwd ls-chat-mini-app all && yarn --cwd ls-auth-mini-app all && yarn --cwd ls-platform all",
    "start:host": "yarn --cwd ls-platform start",
    "start:chat-app": "yarn --cwd ls-chat-mini-app start",
    "start:auth-app": "yarn --cwd ls-auth-mini-app start",
    "run:host:ios": "yarn --cwd ls-platform ios",
    "run:host:android": "yarn --cwd ls-platform android",
    "adbreverse": "adb reverse tcp:8081 tcp:8081 && adb reverse tcp:8083 tcp:8083 && adb reverse tcp:8084 tcp:8084"
  }
}
